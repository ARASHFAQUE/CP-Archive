#include <bits/stdc++.h>
using namespace std;

// For local arrays arr[size] where 1 <= size <= 10000000(1e7)
// const int size = n * 1e7; where n = 1, 2, 3....

int main()
{
    int n; cin >> n;
    int arr[n];

    // 2D Arrays
    int arr[4][4];

    // Size limits of arrays
    // For local arrays arr[size] where 1 <= size <= 100000(1e5)
    // int size = n * 1e5; where n = 1, 2, 3....
}
